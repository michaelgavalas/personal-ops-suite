## What changed

<!-- One or two sentences. What does this do that the repo couldn't do before? -->

## Why

<!-- The reasoning that isn't obvious from the diff. Skip if it genuinely is. -->

## Checks

- [ ] `pnpm lint`, `pnpm check-types`, `pnpm build`, and `pnpm test` pass
- [ ] Schema changes ship with their generated migration in the same commit
- [ ] New environment variables are in the relevant `.env.example` and README
